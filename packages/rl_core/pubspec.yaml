name: rl_core
description: Shared primitives for RemoteLink — domain types, errors, logging, clock.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  characters: ^1.3.0
  meta: ^1.15.0
  unorm_dart: ^0.3.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
