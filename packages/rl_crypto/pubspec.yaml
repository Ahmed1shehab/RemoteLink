name: rl_crypto
description: RemoteLink handshake, pairing, session encryption, and trust store.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  cryptography: ^2.7.0
  meta: ^1.15.0
  rl_core: ^0.1.0
  rl_protocol: ^0.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
