name: brand
description: Regenerates every app icon and bundled logo from the one master artwork.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
